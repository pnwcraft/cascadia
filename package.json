{
  "name": "cascadia-deck-fence-site",
  "version": "1.0.0",
  "private": true,
  "description": "SEO-focused static website for Cascadia Deck & Fence in the Greater Seattle area.",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "check": "npm run check:conflicts && python3 scripts/check_site.py",
    "build": "python3 scripts/build_static.py",
    "check:conflicts": "python3 scripts/check_conflicts.py"
  },
  "devDependencies": {}
}
